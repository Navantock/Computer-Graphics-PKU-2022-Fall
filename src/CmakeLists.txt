add_executable(CGHW3
        main.cpp 
        "mesh.h" "shader.h" "model.h" "camera.h" "draw_ui.h" "particle.h")

target_link_libraries(CGHW3 PRIVATE assimp::assimp glew_s glm stb glfw imgui)
target_include_directories(CGHW3 PRIVATE
        ../third_party/glew/include
        ${CMAKE_CURRENT_BINARY_DIR})

target_compile_features(CGHW3 PRIVATE cxx_std_11)

configure_file(config.h.in ${CMAKE_CURRENT_BINARY_DIR}/config.h)